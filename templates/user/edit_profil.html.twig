{% extends "layout.html.twig" %}

{% block content %}
    <h1>Modifie ton profil, si tu veux</h1>
    <p>email : {{ user.email }}</p>
    <a href="{{ path('user_logout')}}">Se déconnecter</a>
    <a href="{{ path('user_logout')}}">Gestion des photos</a>
{% endblock %}
