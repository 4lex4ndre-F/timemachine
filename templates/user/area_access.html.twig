{% extends "layout.html.twig" %}

{% block content %}
    <div class="container-fluid">
    
    {% if user_manager.admin %}
        
        <h1>Espace Admin</h1>
        <p>email : {{ user.email }}</p>
        <a href="{{ path('user_logout')}}">Se déconnecter</a><br>
    
    {% endif %}
    
    {% if user_manager.member %}
        
        <h1>Espace Membre</h1>
        <p>email : {{ user.email }}</p>
        
        <div class="row justify-content-md-center">
            <div class="col-md-2 col-offset-1">
                <a href="{{ path('user_logout')}}">Se déconnecter</a><br>
                <a href="{{ path('user_logout')}}">Modifier Profil</a><br>
                <a href="{{ path('user_logout')}}">Ajouter Photos</a>
            </div>

            <div class="col-md-8">
                <h3 class="display-5 text-center">Tes Photos</h3>
                {% include 'member/pictures_list.html.twig' %}
            </div>
        </div>
        
    {% endif %}    
    </div><!-- container -->   
{% endblock %}
